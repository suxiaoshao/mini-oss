schema: http://localhost:30002/
documents:
  - "./src/**/*.graphql"
  - "./src/**/*.gql"
generates:
  ./src/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
