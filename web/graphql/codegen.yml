schema: http://localhost:8000/
documents:
  - './src/**/*.graphql'
  - './src/**/*.gql'
generates:
  ./src/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
