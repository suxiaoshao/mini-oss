{
  "name": "admin",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "tslint": "tsc --build tsconfig.json"
  },
  "dependencies": {
    "common": "workspace:^1.0.0",
    "graphql": "workspace:^0.0.0"
  }
}
